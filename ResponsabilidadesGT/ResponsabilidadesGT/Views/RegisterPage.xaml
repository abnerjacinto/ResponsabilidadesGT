<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ResponsabilidadesGT.Views.RegisterPage"
             xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Title="Registrarse">
    <ContentPage.Content>
        <ScrollView
            BindingContext="{Binding Register}">
            <StackLayout Padding="5">
                <Image 
                    Aspect="AspectFit"
                    Source="logo"
                    WidthRequest="200"/>
                <Grid Margin="10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <Label 
                        Grid.Row="0"
                        Grid.Column="0"
                        Text="Nombre de Usuario:" 
                        VerticalOptions="Center"
                    />
                    <Entry
                        Grid.Row="0"
                        Grid.Column="1"
                        Keyboard="Text"
                        Placeholder="Ingrese nombre de usuario"
                        Text="{Binding Name,Mode=TwoWay}">
                    </Entry>
                    <Label 
                        Grid.Row="1"
                        Grid.Column="0"
                        Text="Correo:" 
                        VerticalOptions="Center"
                    />
                    <Entry
                        Grid.Row="1"
                        Grid.Column="1"
                        Keyboard="Email"
                        Placeholder="Ingrese correo."
                        Text="{Binding Email,Mode=TwoWay}">
                    </Entry>
                    <Label 
                        Grid.Row="2"
                        Grid.Column="0"
                        Text="Nuevamente Correo:" 
                        VerticalOptions="Center"
                    />
                    <Entry
                        Grid.Row="2"
                        Grid.Column="1"
                        Keyboard="Email"
                        Placeholder="Re-ingrese correo."
                        Text="{Binding REmail,Mode=TwoWay}">
                    </Entry>
                    <Label 
                        Grid.Row="3"
                        Grid.Column="0"
                        Text="Constraseña:" 
                        VerticalOptions="Center"
                    />
                    <Entry
                        Grid.Row="3"
                        Grid.Column="1"
                        IsPassword="True"
                        Placeholder="Ingrese contraseña."
                        Text="{Binding Password,Mode=TwoWay}">
                    </Entry>
                    <Label 
                        Grid.Row="4"
                        Grid.Column="0"
                        Text="Nuevamente Constraseña:" 
                        VerticalOptions="Center"
                    />
                    <Entry
                        Grid.Row="4"
                        Grid.Column="1"
                        IsPassword="True"
                        Placeholder="Re-ingrese contraseña."
                        Text="{Binding RPassword,Mode=TwoWay}">
                    </Entry>
                </Grid>
                <ActivityIndicator
                    IsRunning="{Binding IsRunning,Mode=TwoWay}">
                </ActivityIndicator>
                <StackLayout
                    Margin="30,0"
                    Orientation="Horizontal">
                    <Button 
                        Text="Registrarse"
                        BackgroundColor="SkyBlue"
                        HeightRequest="46"    
                        TextColor="White"
                        BorderRadius="23"
                        HorizontalOptions="CenterAndExpand"
                        Command="{Binding RegisterCommand}"
                        IsEnabled="{Binding IsEnabled,Mode=TwoWay}">
                    </Button>
                    
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>